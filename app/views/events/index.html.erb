<h1><%= t('pages.events.title.head') %></h1>

<div class="buffer-both">
  <%= link_to t('pages.events.title.create_events'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row buffer-bottom">
  <%= render partial: 'events/event', collection: @events %>
</div>